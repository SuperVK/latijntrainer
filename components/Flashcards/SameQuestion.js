import { Button } from '@material-ui/core'

const SameQuestion = ({ onClick }) => (
	<Button size="small" variant="contained" onClick={onClick}>
		Opnieuw
	</Button>
)



export default SameQuestion;
